import 'dart:ui';

/// Centralized color palette for the light trading theme.
class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);

  // Primary accent
  static const Color primaryNavy = Color(0xFF1A1A2E);

  // Status colors
  static const Color gainGreen = Color(0xFF079455);
  static const Color lossRed = Color(0xFFD92D20);
  static const Color greenTagBg = Color(0xFFECFDF3);
  static const Color redTagBg = Color(0xFFFEF3F2);

  // Text
  static const Color textPrimary = Color(0xFF101828);
  static const Color textSecondary = Color(0xFF667085);
  static const Color textMuted = Color(0xFF98A2B3);

  // Utility
  static const Color divider = Color(0xFFE4E7EC);
  static const Color dragHandle = Color(0xFFC5CAD6);
}
